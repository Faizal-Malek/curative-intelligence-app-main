# (app) Directory

This directory contains authenticated application routes using Next.js App Router conventions.

## Purpose
- Routes that require user authentication
- Protected pages and components
- Main application functionality

## Structure
- Each subdirectory represents a route
- Use `page.tsx` for the main page component
- Use `layout.tsx` for route-specific layouts 