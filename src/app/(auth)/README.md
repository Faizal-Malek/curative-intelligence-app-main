# (auth) Directory

This directory contains authentication-related routes using Next.js App Router conventions.

## Purpose
- Login, signup, and authentication pages
- Password reset flows
- Authentication callbacks
- Public authentication routes

## Structure
- Each subdirectory represents an auth route
- Use `page.tsx` for the main page component
- Use `layout.tsx` for auth-specific layouts 