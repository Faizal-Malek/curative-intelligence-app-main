This migration directory is a placeholder indicating the database schema matches the Prisma data model.

It was created after introspecting the Supabase database using `npx prisma db pull`.

No destructive SQL is included to avoid accidental changes to the existing database.
